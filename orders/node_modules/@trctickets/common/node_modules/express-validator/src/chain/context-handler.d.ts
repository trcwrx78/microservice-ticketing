import { CustomValidator } from '@trctickets/common/node_modules/express-validator/src/base';
import { Optional } from '@trctickets/common/node_modules/express-validator/src/context';
import { ValidationChain } from '@trctickets/common/node_modules/express-validator/src/chain/validation-chain';
export interface ContextHandler<Chain> {
    bail(): Chain;
    if(condition: CustomValidator | ValidationChain): Chain;
    optional(options?: Partial<Optional> | true): Chain;
}
