import { ContextItem } from '@trctickets/common/node_modules/express-validator/src/context-items';
import { Context, Optional } from '@trctickets/common/node_modules/express-validator/src/context';
import { Location } from '@trctickets/common/node_modules/express-validator/src/base';
export declare class ContextBuilder {
    private readonly stack;
    private fields;
    private locations;
    private message;
    private optional;
    setFields(fields: string[]): this;
    setLocations(locations: Location[]): this;
    setMessage(message: any): this;
    addItem(...items: ContextItem[]): this;
    setOptional(options: Optional): this;
    build(): Context;
}
